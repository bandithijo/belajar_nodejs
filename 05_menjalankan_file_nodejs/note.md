# 05 menjalankan file nodejs

secara implisit file `index.js` dapat dipanggil dengan aliasnya berupa `.` (period).

```shell
$ node index.js
```

sama dengan,

```shell
$ node .
```

NodeJS menggunakan module system.
